import { styled } from '@mui/material/styles';
import { GridToolbarContainer } from '@mui/x-data-grid';

const StyledToolbarContainer = styled(GridToolbarContainer)((params) => ({
  padding: '1rem',
}));

export default StyledToolbarContainer;
